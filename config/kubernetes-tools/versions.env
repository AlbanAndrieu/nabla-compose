# Repository-owned pins for the first Kubernetes platform-tools wave.
# Kubara remains pinned separately in config/kubara/VERSION until its upgrade
# is reviewed against the generated ingress/bootstrap contract.
HELM_VERSION=v4.3.0
VAULT_CHART_VERSION=0.34.1
VAULT_APP_VERSION=2.0.4
FALCO_CHART_VERSION=9.1.0
FALCO_APP_VERSION=0.44.1
KUBARA_UPSTREAM_CANDIDATE=0.16.0
