---
# AI services have moved to dedicated app Compose files under apps/.
# Keep this override valid for compatibility with existing compose commands.
services: {}

networks:
  intranet:
    external: true
    name: intranet
