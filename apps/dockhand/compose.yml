---
services:
  dockhand:
    image: fnsys/dockhand:latest
    container_name: dockhand
    restart: unless-stopped
    ports:
      - 3050:3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /mnt/cpool/dockhand:/app/data
