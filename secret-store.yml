stores:
  bitwarden-login:
    version: v1
    url: "http://172.17.0.24:8087/object/item/{{ .remote_ref.key }}"
    method: GET
    headers:
      Content-Type: application/json
      Host: localhost:8087
    json_path: "data.login.{{ .remote_ref.property }}"

  bitwarden-fields:
    version: v1
    url: "http://172.17.0.24:8087/object/item/{{ .remote_ref.key }}"
    method: GET
    headers:
      Content-Type: application/json
      Host: localhost:8087
    json_path: "data.fields[?name=='{{ .remote_ref.property }}'].value"
