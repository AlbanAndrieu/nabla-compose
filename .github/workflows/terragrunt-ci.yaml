---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Terragrunt CI
permissions:
  contents: read

on:
  pull_request:
    branches: ["master"]
    paths:
      - root.hcl
      - infrastructure/**
      - terraform/**

jobs:
  plan:
    runs-on: infra-runners
    permissions:
      contents: read
    outputs:
      drift_detected: ${{ steps.plan.outputs.drift_detected }}
      plan_output: ${{ steps.plan.outputs.plan_output }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup mise
        run: mise install

      - name: Load Secrets
        uses: ./.github/actions/load-secrets
        with:
          op-connect-host: ${{ secrets.OP_CONNECT_HOST }}
          op-connect-token: ${{ secrets.OP_CONNECT_TOKEN }}

      - name: Terragrunt Init
        run: terragrunt run --all -- init -upgrade
        env:
          OP_CONNECT_HOST: ${{ secrets.OP_CONNECT_HOST }}
          OP_CONNECT_TOKEN: ${{ secrets.OP_CONNECT_TOKEN }}

      - name: Terragrunt Plan
        id: plan
        env:
          OP_CONNECT_HOST: ${{ secrets.OP_CONNECT_HOST }}
          OP_CONNECT_TOKEN: ${{ secrets.OP_CONNECT_TOKEN }}
        run: |
          terragrunt run --all --no-color -- plan --detailed-exitcode 2>&1 | tee plan_output.txt

          if [ "${PIPESTATUS[0]}" -eq 2 ]; then
            echo "drift_detected=true" | tee "$GITHUB_OUTPUT"
          else
            echo "drift_detected=false" | tee "$GITHUB_OUTPUT"
          fi

          {
            echo "plan_output<<EOF"
            cat plan_output.txt
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

  comment:
    runs-on: ubuntu-latest
    if: always()
    needs: plan
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Create PR comment
        env:
          GH_TOKEN: "${{ github.token }}"
          PLAN_OUTPUT: "${{ needs.plan.outputs.plan_output }}"
          DRIFT_DETECTED: "${{ needs.plan.outputs.drift_detected }}"
          STATUS: "${{ needs.plan.outputs.drift_detected == 'true' && '⚠️ Changes detected' || '✅ No changes' }}"
        run: |
          {
            echo "## 🤖 Terragrunt Plan Results"
            echo ""
            echo "**Status:** $STATUS"
            echo ""
            echo "<details>"
            echo "<summary>Click to expand plan output</summary>"
            echo ""
            echo '```terraform'
            echo "$PLAN_OUTPUT"
            echo '```'
            echo ""
            echo "</details>"
            echo ""
            echo "---"
            echo "*Workflow run: [#${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*"
          } > comment_body.txt
          gh pr comment "${{ github.event.pull_request.number }}" --body-file comment_body.txt
