---
name: Pre-commit

on:
  pull_request:
    branches: [master]
    paths:
      - ".pre-commit-config.yaml"
      - ".editorconfig"
      - "AGENTS.md"
      - "mise.toml"
      - "**/*.yml"
      - "**/*.yaml"
      - "**/*.json"
      - "**/*.json5"
      - "**/*.sh"
      - "**/*.hcl"
      - "**/*.tofu"
      - "**/Dockerfile*"
      - "Makefile"
      - "Taskfile.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pre-commit-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
        with:
          python-version: "3.13"

      - name: Install pre-commit
        run: python -m pip install --disable-pip-version-check pre-commit

      - name: Cache pre-commit environments
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ runner.os }}-${{ hashFiles('.pre-commit-config.yaml') }}

      - name: Validate generated service catalog contracts
        if: github.event_name == 'pull_request'
        run: |
          pre-commit run service-topology-sync --all-files --show-diff-on-failure
          python -m json.tool catalog/services.json >/dev/null
          python -m json.tool catalog/service-topology.json >/dev/null

      - name: Run repository quality gate on PR changes
        if: github.event_name == 'pull_request'
        env:
          QUALITY_BASE_REF: ${{ github.event.pull_request.base.sha }}
        run: bash scripts/quality-gate.sh

      - name: Run pre-commit on all files
        if: github.event_name != 'pull_request'
        run: pre-commit run --all-files --show-diff-on-failure
