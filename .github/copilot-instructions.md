# GitHub Copilot instructions

Use the repository-wide instructions in `AGENTS.md` as the canonical source of truth.

For Docker Compose work, use `.agents/skills/docker-compose-orchestration/SKILL.md` when additional task-specific guidance is needed.

Keep changes scoped, validate only affected files, never commit secrets, preserve existing homelab conventions, and avoid loading unrelated repository content into context.
